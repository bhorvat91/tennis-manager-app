<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TennisManager.Mobile.ViewModels.Clubs"
             xmlns:models="clr-namespace:TennisManager.Mobile.Models"
             x:Class="TennisManager.Mobile.Views.Clubs.MyClubsPage"
             x:DataType="vm:MyClubsViewModel"
             Title="Moji klubovi">

    <Grid RowDefinitions="Auto,*,Auto">

        <Label Grid.Row="0" Text="{Binding ErrorMessage}" TextColor="Red"
               IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
               Padding="16,8" />

        <ActivityIndicator Grid.Row="0" IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}" HorizontalOptions="Center" />

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Clubs}"
                        EmptyView="Niste član ni jednog kluba."
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:ClubDto">
                    <Border Margin="16,8" Padding="16" StrokeShape="RoundRectangle 8">
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding City}" TextColor="{StaticResource Secondary}" />
                            </VerticalStackLayout>
                            <Label Grid.Column="1" Text="{Binding MemberCount, StringFormat='{0} članova'}"
                                   FontSize="12" TextColor="{StaticResource Secondary}" VerticalOptions="Center" />

                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MyClubsViewModel}}, Path=NavigateToClubCommand}"
                                    CommandParameter="{Binding .}" />
                            </Grid.GestureRecognizers>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="2" Text="Pretraži sve klubove"
                Command="{Binding BrowseClubsCommand}"
                Margin="16" CornerRadius="8"
                BackgroundColor="{StaticResource Primary}" TextColor="White" />

    </Grid>
</ContentPage>
