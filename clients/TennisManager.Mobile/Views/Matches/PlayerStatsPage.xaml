<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TennisManager.Mobile.ViewModels.Matches"
             xmlns:models="clr-namespace:TennisManager.Mobile.Models"
             x:Class="TennisManager.Mobile.Views.Matches.PlayerStatsPage"
             x:DataType="vm:PlayerStatsViewModel"
             Title="Statistika igrača">

    <Grid RowDefinitions="Auto,*">

        <Label Grid.Row="0" Text="{Binding ErrorMessage}" TextColor="Red"
               IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
               Padding="16,8" />

        <ActivityIndicator Grid.Row="0" IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}" HorizontalOptions="Center" />

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Stats}"
                        EmptyView="Nema statistike."
                        SelectionMode="None">
            <CollectionView.Header>
                <Grid ColumnDefinitions="*,Auto,Auto,Auto" Padding="16,8" BackgroundColor="{StaticResource Primary}">
                    <Label Grid.Column="0" Text="Igrač" TextColor="White" FontAttributes="Bold" />
                    <Label Grid.Column="1" Text="P" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" />
                    <Label Grid.Column="2" Text="Z" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" />
                    <Label Grid.Column="3" Text="Uk" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" />
                </Grid>
            </CollectionView.Header>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:PlayerStatsDto">
                    <Grid ColumnDefinitions="*,Auto,Auto,Auto" Padding="16,12">
                        <VerticalStackLayout Grid.Column="0" Spacing="2">
                            <Label Text="{Binding FullName}" FontAttributes="Bold" />
                            <Label Text="{Binding Email}" FontSize="12" TextColor="{StaticResource Secondary}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="1" Text="{Binding Wins}" HorizontalOptions="Center"
                               VerticalOptions="Center" TextColor="Green" FontAttributes="Bold" />
                        <Label Grid.Column="2" Text="{Binding Losses}" HorizontalOptions="Center"
                               VerticalOptions="Center" TextColor="Red" FontAttributes="Bold" />
                        <Label Grid.Column="3" Text="{Binding TotalMatches}" HorizontalOptions="Center"
                               VerticalOptions="Center" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
