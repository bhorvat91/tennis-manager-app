<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TennisManager.Mobile.ViewModels.Auth"
             x:Class="TennisManager.Mobile.Views.Auth.LoginPage"
             x:DataType="vm:LoginViewModel"
             Title="Prijava"
             Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="32" Spacing="20" VerticalOptions="Center">

            <Label Text="🎾" FontSize="64" HorizontalOptions="Center" />
            <Label Text="Tennis Manager" FontSize="28" FontAttributes="Bold"
                   HorizontalOptions="Center" TextColor="{StaticResource Primary}" />
            <Label Text="Upravljanje teniskim klubom" FontSize="14"
                   HorizontalOptions="Center" TextColor="{StaticResource Secondary}" />

            <BoxView HeightRequest="1" Color="{StaticResource Secondary}" Margin="0,10" />

            <Label Text="Email" FontAttributes="Bold" />
            <Entry Placeholder="unesite@email.com"
                   Text="{Binding Email}"
                   Keyboard="Email"
                   ReturnType="Next" />

            <Label Text="Lozinka" FontAttributes="Bold" />
            <Entry Placeholder="Unesite lozinku"
                   Text="{Binding Password}"
                   IsPassword="True"
                   ReturnType="Done"
                   ReturnCommand="{Binding LoginCommand}" />

            <Label Text="{Binding ErrorMessage}"
                   TextColor="Red"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
                   HorizontalOptions="Center" />

            <ActivityIndicator IsRunning="{Binding IsLoading}"
                               IsVisible="{Binding IsLoading}"
                               HorizontalOptions="Center" />

            <Button Text="Prijavi se"
                    Command="{Binding LoginCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBoolConverter}}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    CornerRadius="8"
                    HeightRequest="50" />

            <HorizontalStackLayout HorizontalOptions="Center" Spacing="8">
                <Label Text="Nemate račun?" VerticalOptions="Center" />
                <Button Text="Registrirajte se"
                        Command="{Binding GoToRegisterCommand}"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource Primary}"
                        Padding="0" />
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
